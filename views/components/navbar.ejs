<%- include('./header') %>
  <nav class="w-100 bg-dark p-2">
    <div class="container w-100 d-flex flex-row align-items-center justify-content-between">
      <h4 class="m-0 p-0 text-light fw-lighter text-uppercase">CRUD NODE JS</h4>
      <% if(true==false){ %>
        <h4 class="m-0 p-0 text-light fw-lighter text-uppercase">
          <h4 class="m-0 p-0 text-light bg-dark dropdown-toggle fw-lighter cursor-pointer border p-1 rounded"
            data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fs-3 bi bi-person-fill"></i>
          </h4>
          <ul class="dropdown-menu text-center">
            <li><a class="dropdown-item" href="#">Sair</a></li>
          </ul>
        </h4>
        <% }%>

    </div>
  </nav>


  <div class="d-flex flex-column justify-content-end col-auto mt-5 position-absolute">
    <%if(typeof errors !=='undefined' && typeof errors['message'] !='undefined' ){ %>
      <%if(typeof errors['message']=='string' ){ %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <strong class="d-block">Não foi possível realizar a ação:</strong>
          <%= errors['message']%>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <%}%>
          <%if(typeof errors['message']=='object' ){ %>
            <%for(const message in errors['message']){%>
              <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <strong class="d-block">Não foi possível realizar a ação:</strong>
                <%= errors['message'][message]%>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
              </div>
              <%}%>
                <%}%>
                  <% } %>

                    <!-- <div class="alert alert-warning alert-dismissible fade show" role="alert">
          <strong>Holy guacamole!</strong> You should check in on some of those fields below.
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <strong>Holy guacamole!</strong> You should check in on some of those fields below.
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div> -->
  </div>